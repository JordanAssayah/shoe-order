machine:
  node:
    version: 6.10.1
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - npm install -g swagger
    - cd frontend && npm install

test:
  override:
    - NODE_ENV=test npm run test   # Run the backend tests
    - cd frontend && npm run unit  # Run the frontend tests